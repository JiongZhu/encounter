language: java
jdk:
- oraclejdk8
branches:
  only:
  - master
before_cache:
- rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
- rm -fr $HOME/.gradle/caches/*/plugin-resolution/
cache:
  directories:
  - $HOME/.gradle/caches/
  - $HOME/.gradle/wrapper/
before_install:
- chmod +x gradlew
script:
- ./gradlew clean build -x test
after_success:
  "curl -T ./build/lib/encounter_1.0.jar -u $FTP_USER:$FTP_PASSWORD ftp://39.106.205.37/others/ \\;"